{
  "name": "hydea-clone",
  "version": "1.0.0",
  "scripts": {
    "start": "serve .",
    "build": "mkdir -p dist && cp -r assets index.html dist/"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
